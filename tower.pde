/*void drawShape(RShape r){
  println(r.getPoints().length);
  for (int i=0; i<r.getPoints().length, i++) {
    beginShape();
      vertex(r.getPoints()[i].x, r.getPoints()[i].y);
    endShape();
  }  
}
*/

